# Test 3 Confirmatory Validation Activation Preparation Package V1

## 1. Classification

`PREPARATION_ONLY / IMPLEMENTATION_REVIEW_GO_B0_H0 / ACTIVATION_NOT_READY / NO_AUTHORITY`

This package freezes activation preconditions. It is not an activation package, activation
artifact, grant, runtime checkpoint, permit, witness or scientific record. The final implementation
review result `GO / BLOCKER=0 / HIGH=0` is operational context only. It is never repository or Owner
authority, and it cannot make this package authoritative.

These three preparation documents remediate no code defect and create no activation generator or
activation artifact. They request a later, separate code-only hardening decision on the same four
implementation paths. They may later be anchored only under separate exact staging, commit and push
authority after the applicable gates and cross-family review.

## 2. Exact path bindings

The exact implementation source ref is:

`refs/heads/governance/test3-confirmatory-validation-implementation-v1`

The exact reviewed implementation paths, in order, are:

1. `src/mes_quant/exploration/test3_confirmatory_validation.py`
2. `tests/test_test3_confirmatory_validation.py`
3. `tools/run_test3_confirmatory_validation.py`
4. `tests/test_run_test3_confirmatory_validation.py`

No Git object identity is transcribed here. Any byte or code change in any of the four paths reopens
all applicable gates and requires a fresh cross-family review.

The following two exact repository paths form one inseparable co-ratified contract:

1. `docs/research/TEST3_CONFIRMATORY_VALIDATION_PROTOCOL_PREPARATION_V1.md`
2. `docs/research/TEST3_CONFIRMATORY_VALIDATION_PROTOCOL_SIGNED_BOOTSTRAP_ERRATUM_V1.md`

The exact Owner-ratification path is:

- `docs/research/TEST3_CONFIRMATORY_VALIDATION_PROTOCOL_OWNER_RATIFICATION_V2.md`

The following exact paths are bound only as `TOOLING_CAPABILITY_ONLY`:

1. `tools/prepare_test3_confirmatory_validation_prerequisites.py`
2. `tests/test_prepare_test3_confirmatory_validation_prerequisites.py`
3. `docs/research/TEST3_CONFIRMATORY_VALIDATION_TOOLING_BINDING_V1.json`

Authority is never inferred from a filename, a repository path, the tooling binding, a review
verdict or the existence of this package.

## 3. Load-bearing precondition

Before implementation acceptance, Grant 1, `C0`, reservation, either permit, either fit, a protected
read, a witness or Validation, an independently authorized deterministic operation must perform all
of the following in order:

1. rerun the historical-parent verification required by the co-ratified contract;
2. bind the exact bytes of the Protocol and Signed Bootstrap Erratum as one contract; and
3. exactly match the resulting complete machine-generated parent-binding block to the complete
   machine-generated block bound by Owner Ratification V2.

A path citation, prose assertion, review result or manually transcribed identity cannot satisfy this
precondition. Mismatch, absence, ambiguity or incomplete output stops before implementation
acceptance and before every scientific or protected operation named above.

## 4. Closed future causal sequence

The only proposed future sequence is the following. Completion of one step never implies, grants or
authorizes the next.

1. Rerun the authorized deterministic historical-parent verification, bind the exact co-ratification
   bytes and exactly match the machine-generated parent-binding block.
2. Under separate exact Owner authority, harden the same four implementation paths for LOW 3 and
   LOW 4 only, run their tests, obtain fresh cross-family `GO / BLOCKER=0 / HIGH=0`, and anchor the
   exact reviewed result under separate authority.
3. Obtain a later, separate Owner activation decision.
4. Obtain a separate Owner Grant 1 that cites and binds Protocol Section 0.1.5.
5. Run a separately authorized data-free `C0`: bind the live execution-runtime identity, immediately
   re-record it and require exact equality, then replay the ratification-bound golden fixture.
6. Create the confirmatory reservation.
7. Attempt exactly BASE and then HAR. Each permit is consumed at attempt, not success.
8. Independently verify both coefficient seals and both full-outer-TRAIN model-local Duan seals.
9. Only after both model seals verify, obtain a separate Owner Grant 2 that cites and binds Protocol
   Sections 0.1.5 and 4.
10. Run a separately authorized data-free `C0V`: require scoring-runtime identity equality to `C0`,
    immediately re-record and require equality again, and replay the same golden fixture.
11. Durably create exactly one create-once Validation-opening witness.
12. Build and seal the complete Validation request set before any provider lookup.
13. Create exactly one immutable Validation materialization.
14. Pass integrity checks.
15. Pass structural-support checks.
16. Score exactly once.
17. Write exactly one no-retry terminal record.

Final Test remains `SEALED`. No step opens Final Test, Test 3b or Test 4.

## 5. Mandatory future LOW 3 hardening

A future code-only slice must provide one closed end-to-end scientific scoring orchestrator. The
orchestrator must compel deployment precheck and `ValidationSupport` before any scored tier. It must
evaluate PASS from the actual internally selected PRIMARY `BootstrapBlockResult` for block length 5,
never from a caller-supplied bare float or caller-selected result.

It must run and record all frozen block lengths in exact order `(5, 1, 20)`, with exactly 2,000
replicates for each block length. Block lengths 1 and 20 remain mandatory diagnostics. They may not
be skipped, promoted, caller-selected, substituted for PRIMARY, or used to rescue block length 5.
The terminal classifier must not reach any scored tier when deployment precheck or Validation
support is absent.

## 6. Mandatory future LOW 4 hardening

A future code-only slice must define one immutable `ValidationMaterialization` with one identity. It
must carry, in exact order and without caller reconstruction:

- session identity and decision time;
- request, target and predictor status;
- the common mask;
- actual `RV_FWD_60`;
- exact `X60`, `X120`, `X240` and design order;
- BASE and HAR predictions; and
- the single materialization identity.

Chronology, the common mask, ACF rows, row losses, session aggregates and the bootstrap must all
rebind to the same exact row set, row order and materialization identity. Missing, duplicate,
permuted, session-reassigned or otherwise mismatched rows must be rejected before any forecast or
scoring.

## 7. Erratum semantics retained

For every bootstrap replicate, the denominator must be strictly positive and finite. Every stored
`D_star` must be finite; its sign is unconstrained. The strict `D > 0` criterion and every other
frozen scientific constant and criterion remain unchanged. Negative or zero finite replicates are
valid scored outcomes and cannot be relabelled as integrity failures solely because of sign.

## 8. Adversarial test contract for the later hardening

The later four-path code-only hardening must reject, before scoring where applicable:

- missing deployment precheck or missing Validation support;
- substitution, promotion or selection of a diagnostic block result;
- a caller-supplied PRIMARY float;
- row permutation, duplication, deletion or session reassignment;
- common-mask drift or materialization-identity drift;
- fake row, session, attempt, permit, access or fit counts;
- runtime-identity or golden-replay drift;
- forged Grant 1 or Grant 2;
- any protected access before the durable Validation-opening witness; and
- forecast, QLIKE or bootstrap work on underpowered inputs.

Tests must prove that 19 sessions never reach forecast, QLIKE or bootstrap; that undefined support
at any required ACF lag never reaches those operations; and that chronology, common mask, ACF rows,
row losses, session aggregates and bootstrap operands all carry and rebind to one materialization
identity.

## 9. Scope and prohibitions

This preparation slice authorizes no implementation edit. It creates no activation generator or
artifact, Grant 1, Grant 2, `C0`, `C0V`, reservation, permit, coefficient seal, Duan seal, witness,
request set, materialization, access or scientific terminal record.

It authorizes no data, provider, target or evidence access; no model import, fit or scoring; no
Validation or Final Test; no Test 3b or Test 4; no server, cloud, OIDC or signing operation; and no
CI, pull request, merge or `main` mutation. Staging, commit and push of these preparation artifacts
require separate exact Owner authority after gates and review.

## 10. Decision boundary

This package requests only two later Owner choices: whether to accept this preparation boundary and
whether to issue a separate exact authorization for one four-path code-only LOW 3/LOW 4 hardening
slice. Neither choice is made by this package. Accepting preparation is not activation, Grant 1,
Grant 2, `C0`, `C0V`, reservation, Validation or scientific execution.
