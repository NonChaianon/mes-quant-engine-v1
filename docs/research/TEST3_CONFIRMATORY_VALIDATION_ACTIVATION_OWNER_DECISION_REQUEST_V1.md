# Test 3 Confirmatory Validation Activation Owner Decision Request V1

## 1. Classification

`PREPARATION_ONLY / IMPLEMENTATION_REVIEW_GO_B0_H0 / ACTIVATION_NOT_READY / NO_AUTHORITY`

This request carries no authority. The implementation review result
`GO / BLOCKER=0 / HIGH=0` is operational context only, never repository or Owner authority. No
filename, path, review outcome, config or tooling artifact can substitute for an Owner decision.

## 2. Decision requested

The Owner is asked to decide only:

1. whether to accept the preparation boundary recorded by this request, the companion package and
   the machine-readable preconditions; and
2. whether the team should next prepare a separate exact Owner authorization for one code-only
   hardening slice on the same four paths, limited to LOW 3 scoring-orchestration closure and LOW 4
   immutable Validation-materialization closure.

An `APPROVE` decision on this request does not activate anything. It cannot by itself authorize the
four-path hardening mutation: that work requires a later separate exact Owner authorization naming
its base, ref, paths, mutation budget, gates and prohibitions. It also cannot authorize historical
verification, implementation acceptance, Grant 1, Grant 2, `C0`, `C0V`, reservation, permit, fit,
seal, witness, access, scoring or Validation.

A `REJECT` decision freezes this preparation package. Rejection permits no repair, successor,
implementation change or activation action without fresh explicit Owner authority.

This document deliberately contains no activation-approval sentence.

## 3. Exact materials presented for the decision

The preparation set consists of:

1. `configs/research/test3_confirmatory_validation_activation_preconditions_v1.json`
2. `docs/research/TEST3_CONFIRMATORY_VALIDATION_ACTIVATION_PREPARATION_PACKAGE_V1.md`
3. `docs/research/TEST3_CONFIRMATORY_VALIDATION_ACTIVATION_OWNER_DECISION_REQUEST_V1.md`

The exact implementation source ref is:

`refs/heads/governance/test3-confirmatory-validation-implementation-v1`

The exact reviewed implementation paths, in order, are:

1. `src/mes_quant/exploration/test3_confirmatory_validation.py`
2. `tests/test_test3_confirmatory_validation.py`
3. `tools/run_test3_confirmatory_validation.py`
4. `tests/test_run_test3_confirmatory_validation.py`

Any byte or code change in those paths reopens all applicable gates and requires a fresh
cross-family review.

The operative scientific contract is the inseparable co-ratified pair:

1. `docs/research/TEST3_CONFIRMATORY_VALIDATION_PROTOCOL_PREPARATION_V1.md`
2. `docs/research/TEST3_CONFIRMATORY_VALIDATION_PROTOCOL_SIGNED_BOOTSTRAP_ERRATUM_V1.md`

The Owner-ratification path is:

- `docs/research/TEST3_CONFIRMATORY_VALIDATION_PROTOCOL_OWNER_RATIFICATION_V2.md`

The reviewed prerequisite capability is bound only as `TOOLING_CAPABILITY_ONLY`:

1. `tools/prepare_test3_confirmatory_validation_prerequisites.py`
2. `tests/test_prepare_test3_confirmatory_validation_prerequisites.py`
3. `docs/research/TEST3_CONFIRMATORY_VALIDATION_TOOLING_BINDING_V1.json`

## 4. Non-negotiable parent-binding gate

Before implementation acceptance or any later Grant 1, `C0`, reservation, permit, fit, protected
read, witness or Validation action, a separately authorized deterministic operation must:

1. rerun the required historical-parent verification;
2. bind the exact bytes of both co-ratified documents; and
3. exactly match the complete machine-generated parent-binding block to the complete block bound by
   Owner Ratification V2.

Failure or ambiguity stops before all scientific and protected actions. This request does not run
that operation and does not claim that this gate has passed.

## 5. What the requested later hardening must close

LOW 3 requires one closed end-to-end scoring orchestrator. It must compel deployment precheck and
`ValidationSupport`, internally select the actual PRIMARY block-length-5 `BootstrapBlockResult`,
and reject caller-supplied primary floats or caller-selected block results. It must run and record
exactly `(5, 1, 20)` with exactly 2,000 replicates each. The 1-session and 20-session runs remain
mandatory diagnostics and may never be skipped, promoted or used to rescue the 5-session result.
No scored terminal tier is reachable without the deployment and support gates.

LOW 4 requires one immutable `ValidationMaterialization` carrying the exact ordered session identity
and decision time, request/target/predictor status, common mask, actual `RV_FWD_60`, exact
`X60`/`X120`/`X240` design order, BASE and HAR predictions, and one materialization identity.
Chronology, common mask, ACF rows, row losses, session aggregates and bootstrap must all rebind to
the same exact row set, order and identity. Missing, duplicate, permuted, session-reassigned or
mismatched rows stop before forecast or scoring.

The future tests must reject missing precheck/support, diagnostic substitution, caller-supplied
primary values, row-set/order drift, common-mask drift, fake counts, runtime/golden drift, forged
grants, access before the durable witness and any scoring on underpowered inputs. They must prove
that 19 sessions and undefined support at any required ACF lag never reach forecast, QLIKE or
bootstrap, and that every operand rebinds to one materialization identity.

## 6. Future sequence protected by this preparation

The package freezes, but does not authorize, this order:

1. historical verification, co-ratification-byte binding and exact parent-block match;
2. separate LOW 3/LOW 4 four-path authority, hardening, tests, fresh cross-family GO and anchor;
3. later separate Owner activation decision;
4. separate Owner Grant 1 citing and binding Protocol Section 0.1.5;
5. data-free `C0` runtime binding, immediate equality rerecord and golden replay;
6. reservation;
7. exactly BASE then HAR, with each permit consumed at attempt;
8. independent verification of both coefficient seals and both full-outer-TRAIN model-local Duan
   seals;
9. separate Owner Grant 2 citing and binding Sections 0.1.5 and 4, only after both seals verify;
10. data-free `C0V` equality to `C0`, immediate equality rerecord and the same golden replay;
11. durable create-once Validation-opening witness;
12. sealed complete request set;
13. one immutable Validation materialization;
14. integrity;
15. structural support;
16. exactly one score; and
17. exactly one no-retry terminal.

No step implies the next. Final Test remains `SEALED`.

The erratum remains controlling within this sequence: each bootstrap denominator must be strictly
positive and finite, each stored `D_star` must be finite with sign unconstrained, and the frozen
strict `D > 0` gate and all other scientific constants remain unchanged.

## 7. No code or activation effect

These three preparation documents remediate no code defect. They create no activation generator or
artifact. They only freeze preconditions and request a later separate four-path code-only LOW 3 and
LOW 4 hardening decision.

This request prohibits implementation edits in this slice; activation generator or artifact
creation; Grant 1 or Grant 2; `C0` or `C0V`; reservation, permit, seal or witness creation; data,
provider, target or evidence access; model import, fit or scoring; Validation; Final Test; Test 3b;
Test 4; server, cloud, OIDC or signing work; CI; pull request; merge; and `main` mutation.

Staging, commit or push of this preparation set requires separate exact Owner authority after its
gates and review. This decision request grants none of those actions.
