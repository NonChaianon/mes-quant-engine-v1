# Test 3 G3-P TRAIN Pre-Fit Owner Authorization V1

Authorization ID: `AUTH_TEST3_G3P_TRAIN_PREFIT_20260825`

Authorization token: `OWNER_AUTHORIZED_TEST3_G3P_TRAIN_PREFIT_20260825`

Gate literal: `G3P_TEST3_TRAIN_TARGET_SUPPORT_PREFIT`

Status: `OWNER AUTHORIZED / ONE-SHOT / TRAIN TARGET-AWARE / ZERO FIT`

Owner decision date: 2026-08-25 (Asia/Bangkok)

Exact base commit/tree: `a1ea24445f575c5d267d6bfe410cc7acd034b74f` /
`ce20f1ecafb3a58fece522cb04048ffef9b628a8`

Execution branch: `research/test3-g3p-pre-fit-v1`

## Owner instruction

The Owner authorizes Test 3 G3-P TRAIN-only target/support pre-fit after the canonical G2-P
proven-defect successor passed at evidence commit `a1ea244`. Codex may prepare and review a
code-only package, commit and push it, and then execute one exact G3-P one-shot. The run may
read only the frozen outer-TRAIN target/path sources required by the ratified protocol,
construct and seal the exact `RV_FWD_60` target ledger, reconcile Cell 12, reproduce and
bind the passed G2-P predictor ledger, construct the frozen common eligibility mask, and
report fold boundaries, harmonic/design rank, ACF/ESS, and structural support.

Model fit, coefficient, forecast, Duan factor, QLIKE, bootstrap, economic diagnostic,
outer Validation, Final Test, live/database/UI coupling, merge, and any scientific change
remain forbidden.

## Irreversible target-space rule

`TARGET_SPACE_003` is irreversibly `CONSUMED` immediately before the first numeric
target/path read or target construction. Consumption is independent of whether G3-P later
passes, stops underpowered, or fails. The single proven-defect repair successor was already
used by G2-P; this authorization creates no new retry, repair, replacement, or search slot.

Before numeric target/path access, the runner must exclusively persist three ordered facts:

1. the authorization reservation binding this document/token and the exact execution
   commit/tree/branch;
2. the complete outer-TRAIN request-set hash/count with Validation and Final request counts
   both zero;
3. the `TARGET_SPACE_003` consumption witness binding that sealed request set.

The third witness must also bind this authorization-document SHA and the authorization-
reservation SHA directly. A failure record may claim target consumption only after strict
verification of the complete reservation -> request -> target witness chain. Every failure
keeps Validation/Final access status conservative (`NOT_ATTESTED_FAIL_CLOSED`) and closes
Test 3 with no retry, replacement, repair, or G3-F path.

An existing-file collision or incomplete predecessor/source binding fails closed. No output
may overwrite a prior record.

## Authorized sources and projections

The one-shot may bind the exact frozen bytes for raw DBN, Cell 8, Cell 10, Cell 12, and
Cell 14. Before target consumption it may read only Cell 8 outer-TRAIN controls and Cell 14
outer-TRAIN controls, the three frozen volatility predictors, and the target-independent
calendar fields needed by the frozen harmonic. After the request set and consumption
witness are persisted, it may decode the canonical DBN and read only outer-TRAIN Cell 10/12
target/path projections. Every provider lookup must be a key in the sealed request set.

Validation and Final-Test target/request counters must be zero before provider access and
remain zero. Raw target values, predictor values, decision identities, and per-row outputs
may exist only in memory and must not be persisted.

The raw DBN and Cell 12 inputs must be byte-identical local cache snapshots under
`artifacts/cache/source_v1`; the one-shot must not depend on a cloud/FUSE mount. Recomputing
the frozen decoded Cell 2 identity necessarily materializes the full canonical decoded
frame. This is an identity-only decode, not a protected target lookup: target-row counters
mean application-exposed sealed request-key lookups. The record must state that the decoded
frame was not partition-filtered and may not claim otherwise.

## Frozen mechanics

The package must reproduce without reinterpretation:

- the exact 60 post-decision one-minute log returns and `RV_FWD_60` target;
- complete-ledger handling of `TARGET_ZERO_VARIANCE` and immediate fail-closed handling of
  other target contract defects;
- exact Cell 12 identity/status/path reconciliation;
- the passed G2-P predictor status counts and ordered hashes;
- the exact common eligibility rule, Cell 8 folds, early-close-aware harmonic, ordered
  `RVBASE001`/`RVHAR001` designs, full-rank and session-support checks;
- within-session lags 1..8, frozen overlap null, design effect, and ESS for both folds and
  pooled disjoint OOF rows.

No fit-equivalent call may be used to establish rank. The runner must mechanically block
the frozen OLS, QLIKE, bootstrap, Duan/back-transform, and continuation surfaces throughout
the target-aware run.

## Exact package allowlist

The execution commit must be one direct child of `a1ea244` and differ in exactly these five
paths:

1. `docs/research/TEST3_G3P_TRAIN_PREFIT_AUTHORIZATION_V1.md`
2. `docs/research/TEST3_G3P_TRAIN_PREFIT_PACKAGE_V1.md`
3. `src/mes_quant/exploration/test3_g3p_pre_fit.py`
4. `tests/test_test3_g3p_pre_fit.py`
5. `tools/run_test3_g3p_pre_fit.py`

The frozen protocol/budget/L0, G2/G2-P packages and evidence, upstream scientific modules,
Validation, and Final-Test surfaces must remain byte-identical to the base.

## Exact Owner command

```text
.venv/bin/python -I -B tools/run_test3_g3p_pre_fit.py \
  --gate G3P_TEST3_TRAIN_TARGET_SUPPORT_PREFIT \
  --authorization-token OWNER_AUTHORIZED_TEST3_G3P_TRAIN_PREFIT_20260825 \
  --raw-dbn "/Users/nonchaianon/Documents/Codex/MES_Quant_Engine_V1/artifacts/cache/source_v1/MES_2019_2026_1m.dbn.zst" \
  --cell8 "/Users/nonchaianon/Documents/Codex/MES_Quant_Engine_V1/artifacts/cache/source_v1/cell8_purged_split_assignments_v1.parquet" \
  --cell10 "/Users/nonchaianon/Documents/Codex/MES_Quant_Engine_V1/artifacts/cache/source_v1/cell10_point_in_time_economic_labels_v1.parquet" \
  --cell12 "/Users/nonchaianon/Documents/Codex/MES_Quant_Engine_V1/artifacts/cache/source_v1/cell12_development_path_outcomes_v1.parquet" \
  --cell14-features "/Users/nonchaianon/Documents/Codex/MES_Quant_Engine_V1/artifacts/runs/cell14_20260809T175203Z/cell14_development_point_in_time_features_v1.parquet"
```

The execution commit must first pass targeted/full pytest, changed-file Ruff, ancestry and
allowlist checks, exact `origin` upstream equality, the untracked-import firewall, isolated
`-I -B` runtime/module-origin checks, and independent Claude Opus exact-package review.

## Outcome boundary

A passing structural pre-fit record may only make a separate G3-F authorization request.
It does not authorize a fit. `UNDERPOWERED_STOP` or `INVALID_EVIDENCE` ends Test 3. Any
execution failure after reservation also ends this authorization and permits no retry.
Reservation, request-set witness, consumption witness, and aggregate record or typed failure
must be committed and pushed promptly in a separate evidence commit. No merge is authorized.
If failure occurs before the consumption witness, the project slot fact remains
`LOCKED / RESERVED` but this authorization still has no retry; after the witness it remains
irreversibly `CONSUMED`.
